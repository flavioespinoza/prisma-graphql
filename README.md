# prisma-graphql

```bash
echo "# prisma-graphql" >> README.md
git init
git add README.md
git commit -m "first commit"
git remote add origin https://github.com/flavioespinoza/prisma-graphql.git
git push -u origin master
```
